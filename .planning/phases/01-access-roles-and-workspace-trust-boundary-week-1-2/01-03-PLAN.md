---
phase: 01-access-roles-and-workspace-trust-boundary-week-1-2
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - web/src/modules/auth/types/auth.ts
  - web/src/modules/auth/api/auth.ts
  - web/src/modules/auth/components/session-guard.tsx
  - web/src/modules/auth/components/guest-guard.tsx
  - web/src/config/sidebar-nav.config.ts
  - web/src/components/layout/sidebar.tsx
  - web/src/app/login/page.tsx
  - web/src/app/(dashboard)/access-denied/page.tsx
  - web/src/app/invite/[token]/page.tsx
  - web/src/app/invite/expired/page.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Restricted features are hidden from navigation for unauthorized roles.
    - Same-workspace forbidden actions show standardized Access denied UX.
    - Expired sessions route to login with Session expired message.
    - Expired invite links show dedicated Invite expired page with request-new-invite CTA.
    - Successful invite acceptance routes user into invited workspace dashboard.
  artifacts:
    - path: web/src/config/sidebar-nav.config.ts
      provides: Role-aware navigation metadata for hidden restricted menu items.
    - path: web/src/app/(dashboard)/access-denied/page.tsx
      provides: Forbidden page UX with back-to-dashboard CTA.
    - path: web/src/app/invite/[token]/page.tsx
      provides: Invite acceptance flow with transfer warning confirmation path.
    - path: web/src/app/invite/expired/page.tsx
      provides: Dedicated expired-invite UX.
    - path: web/src/modules/auth/components/session-guard.tsx
      provides: Deterministic expired-session redirect behavior.
  key_links:
    - from: web/src/components/layout/sidebar.tsx
      to: web/src/config/sidebar-nav.config.ts
      via: role-aware item filtering
      pattern: NAV_ITEMS
    - from: web/src/app/invite/[token]/page.tsx
      to: web/src/modules/auth/api/auth.ts
      via: invite accept API calls
      pattern: invite
    - from: web/src/modules/auth/components/session-guard.tsx
      to: web/src/app/login/page.tsx
      via: redirect query/message state
      pattern: session
---

<objective>
Implement the frontend trust-boundary UX for roles, forbidden access, and invite/session edge states.

Purpose: Ensure phase-1 security decisions are visible and understandable to users, not just enforced on the backend.
Output: Role-aware navigation, access denied page, invite acceptance and expired invite pages, and deterministic session-expiry messaging.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@design/crm.pen
@.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-01-PLAN.md
@.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-02-PLAN.md
@web/src/modules/auth/api/auth.ts
@web/src/modules/auth/components/session-guard.tsx
@web/src/components/layout/sidebar.tsx
@web/src/config/sidebar-nav.config.ts

<interfaces>
From web/src/config/sidebar-nav.config.ts:
```ts
interface NavItem {
  label: string;
  href: string;
  icon?: LucideIcon;
}
```

From web/src/modules/auth/api/auth.ts:
```ts
export async function getSession() {
  const response = await authApiClient.get<SessionResponse>("/api/v1/auth/me")
  return response.data
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add role-aware auth contracts for frontend gating</name>
  <files>web/src/modules/auth/types/auth.ts, web/src/modules/auth/api/auth.ts, web/src/config/sidebar-nav.config.ts, web/src/components/layout/sidebar.tsx</files>
  <behavior>
    - Test 1: nav config can express required roles and restricted items are hidden for disallowed roles.
    - Test 2: canonical role labels are used in UI metadata and never free-text workspace-custom labels.
  </behavior>
  <action>Extend auth types to include canonical role contract from backend. Add role requirements to nav config and filter sidebar rendering by current authenticated role. Hide restricted menu items entirely per locked decision.</action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <done>Sidebar and role-aware UI contracts enforce hidden restricted navigation consistently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build forbidden, invite-accept, and invite-expired UX routes</name>
  <files>web/src/app/(dashboard)/access-denied/page.tsx, web/src/app/invite/[token]/page.tsx, web/src/app/invite/expired/page.tsx, web/src/modules/auth/api/auth.ts, web/src/modules/auth/components/session-guard.tsx, web/src/modules/auth/components/guest-guard.tsx, web/src/app/login/page.tsx</files>
  <behavior>
    - Test 1: same-workspace 403 states route to access-denied page with back-to-dashboard CTA.
    - Test 2: expired invite token routes to dedicated invite-expired page with request-new-invite CTA.
    - Test 3: existing-user cross-workspace invite acceptance requires explicit transfer confirmation.
    - Test 4: expired session redirects to login with Session expired message.
  </behavior>
  <action>Create access-denied page and invite routes aligned with design contract. Wire invite accept page to backend invitation APIs from Plan 01-02, including explicit transfer warning confirmation for existing users. Update auth guards/login page to surface Session expired state after one silent refresh attempt.</action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <done>Frontend exposes all required forbidden/session/invite edge-state UX and compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify trust-boundary UX flows manually</name>
  <what-built>Role-hidden navigation, access denied page, invite acceptance flow, invite expired page, and expired-session redirect messaging.</what-built>
  <action>Run a final manual UX verification pass after automated lint checks, confirm all locked trust-boundary behaviors, and capture any mismatches for follow-up fixes before approval.</action>
  <how-to-verify>
    1. Login as a role without PM/Admin capabilities and verify restricted menu items are hidden.
    2. Trigger a same-workspace forbidden action and confirm access-denied page shows standardized message and dashboard CTA.
    3. Open an expired invite link and confirm invite-expired page and request-new-invite CTA are shown.
    4. Expire session cookies and confirm one silent refresh attempt occurs, then login redirect with Session expired message.
    5. Accept invite for an existing account from another workspace and confirm explicit transfer warning/confirmation appears before completion.
  </how-to-verify>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <done>All five manual trust-boundary checks pass or issues are documented with exact failing steps for targeted fixes.</done>
  <resume-signal>Type approved if all checks pass, or provide failing step numbers with observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- Run frontend lint after all route and guard updates.
- Manually validate role visibility, access denied UX, invite edge states, and session-expiry message behavior.
</verification>

<success_criteria>
- AUTH-04 forbidden and role-visibility UX is explicit and consistent.
- AUTH-03 invite acceptance and expiry UX fully matches locked decisions.
- AUTH-02 expired-session UX is deterministic and user-readable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-03-SUMMARY.md`
</output>
