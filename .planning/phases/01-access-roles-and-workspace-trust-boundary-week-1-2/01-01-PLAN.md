---
phase: 01-access-roles-and-workspace-trust-boundary-week-1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/features/auth/models.py
  - backend/app/features/auth/schemas.py
  - backend/app/features/auth/security.py
  - backend/app/features/auth/service.py
  - backend/app/features/auth/routes.py
  - backend/app/features/shared/dependencies.py
  - backend/tests/integration/auth/test_session_flow.py
  - web/src/modules/auth/types/auth.ts
  - web/src/modules/auth/api/auth.ts
  - web/src/modules/auth/components/session-guard.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
must_haves:
  truths:
    - User can sign up and log in inside a workspace using canonical role labels.
    - Session survives refresh via one silent refresh attempt and expires deterministically.
    - Current-device logout revokes only the current refresh session.
  artifacts:
    - path: backend/app/features/auth/schemas.py
      provides: Canonical role enum and request/response contracts with typed role field.
    - path: backend/app/features/auth/service.py
      provides: Atomic login/register/session lifecycle operations.
    - path: backend/app/features/auth/routes.py
      provides: Auth context with role and token validation contract.
    - path: web/src/modules/auth/api/auth.ts
      provides: Auth-safe query retry policy and session request contract.
    - path: backend/tests/integration/auth/test_session_flow.py
      provides: Automated coverage for login refresh logout and invalid session paths.
  key_links:
    - from: backend/app/features/auth/routes.py
      to: backend/app/features/auth/service.py
      via: get_current_auth_context calling validate_active_session
      pattern: validate_active_session
    - from: backend/app/features/auth/service.py
      to: backend/app/features/auth/models.py
      via: refresh session creation rotation revocation
      pattern: RefreshSession
    - from: web/src/modules/auth/components/session-guard.tsx
      to: web/src/modules/auth/api/auth.ts
      via: useSessionQuery
      pattern: useSessionQuery
---

<objective>
Stabilize the authentication/session core and role contract so all phase-1 invite and policy work builds on one trusted foundation.

Purpose: Remove auth drift (free-text roles, retry loops, partial commits) before introducing invitation and policy flows.
Output: Hardened backend auth/session contract plus frontend session retry behavior and backend integration tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-RESEARCH.md
@backend/app/features/auth/models.py
@backend/app/features/auth/schemas.py
@backend/app/features/auth/routes.py
@backend/app/features/auth/service.py
@backend/app/features/shared/dependencies.py
@web/src/modules/auth/api/auth.ts
@web/src/lib/api/client.ts

<interfaces>
From backend/app/features/auth/routes.py:
```python
@dataclass(frozen=True)
class AuthContext:
    user_id: str
    workspace_id: str
```

From backend/app/features/auth/schemas.py:
```python
class UserResponse(BaseModel):
    id: str
    email: EmailStr
    phone_number: str
    role_description: str
    is_verified: bool
    workspace_id: str
```

From web/src/modules/auth/api/auth.ts:
```ts
export function useSessionQuery(enabled = true) {
  return useQuery({
    queryKey: SESSION_QUERY_KEY,
    queryFn: getSession,
    enabled,
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock auth/session behavior with failing integration tests first</name>
  <files>backend/tests/integration/auth/test_session_flow.py</files>
  <behavior>
    - Test 1: successful login sets access_token and refresh_token cookies and returns authenticated user.
    - Test 2: refresh with valid refresh cookie rotates token and keeps user authenticated.
    - Test 3: logout revokes only current refresh session and clears cookies.
    - Test 4: invalid or expired refresh token returns 401 and does not authenticate session.
  </behavior>
  <action>Create integration tests for login, refresh, me, and logout endpoints using existing app/session fixtures or minimal local fixtures. Keep tests workspace-scoped and assert cookie semantics from locked decisions (remember-me unchecked uses session cookie, checked uses persistent behavior).</action>
  <verify>
    <automated>cd backend && pytest tests/integration/auth/test_session_flow.py -q</automated>
  </verify>
  <done>Auth/session tests fail first against current behavior, then pass after implementation in Task 2 and Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalize roles and make auth write paths atomic</name>
  <files>backend/app/features/auth/models.py, backend/app/features/auth/schemas.py, backend/app/features/auth/service.py, backend/app/features/auth/routes.py, backend/app/features/shared/dependencies.py</files>
  <behavior>
    - Test 1: workspace creator is always stored as Admin/Owner canonical role.
    - Test 2: role values outside canonical v1 set are rejected at schema boundary.
    - Test 3: login/register/session issue paths avoid partial commit behavior on failures.
  </behavior>
  <action>Introduce canonical role enum and migrate API contracts from free-text role_description to canonical role values while preserving UI labels. Enforce creator default role as Admin/Owner and include role in auth context and validate-token headers. Use explicit transaction boundaries for login/register plus refresh session lifecycle writes. Keep logout semantics as current-device only per locked decision.</action>
  <verify>
    <automated>cd backend && pytest tests/integration/auth/test_session_flow.py -q</automated>
  </verify>
  <done>Auth models/schemas/routes/services use canonical role contract and pass session integration tests with no partial write regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden frontend session query behavior for deterministic expiry UX</name>
  <files>web/src/modules/auth/types/auth.ts, web/src/modules/auth/api/auth.ts, web/src/modules/auth/components/session-guard.tsx</files>
  <behavior>
    - Test 1: session query does not repeatedly retry 401/403 auth failures.
    - Test 2: session guard performs one silent refresh attempt via interceptor path, then redirects with Session expired message state.
  </behavior>
  <action>Update auth types and session query configuration so auth-sensitive queries do not loop retries. Extend SessionGuard redirect to include deterministic expired-session signal consumable by login page messaging, matching locked UX behavior.</action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <done>Frontend auth guard behaves deterministically on expired sessions and lint passes.</done>
</task>

</tasks>

<verification>
- Run backend auth integration tests for login, refresh, me, and logout paths.
- Validate role claim/header propagation via validate-token contract in tests.
- Run frontend lint to ensure session guard and auth API typing changes are valid.
</verification>

<success_criteria>
- AUTH-01 and AUTH-02 baseline flows are stable under automated tests.
- Canonical role contract exists and is used by auth context instead of free-text role names.
- Expired sessions produce a deterministic redirect path with an explicit expired reason.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-01-SUMMARY.md`
</output>
