---
phase: 01-access-roles-and-workspace-trust-boundary-week-1-2
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/app/features/auth/models.py
  - backend/app/features/auth/schemas.py
  - backend/app/features/auth/service.py
  - backend/app/features/auth/routes.py
  - backend/app/features/auth/security.py
  - backend/tests/integration/auth/test_invitation_flow.py
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - Admin/Owner and PM/Team Lead can invite users with a selected canonical role.
    - Pending invites can be listed, resent, role-edited, and revoked before acceptance.
    - Invite acceptance supports new user signup and explicit transfer confirmation for existing users from another workspace.
    - Expired invites are rejected as expired and do not activate access.
  artifacts:
    - path: backend/app/features/auth/models.py
      provides: Invitation state machine fields with expiry and lifecycle timestamps.
    - path: backend/app/features/auth/routes.py
      provides: Invitation management and acceptance endpoints.
    - path: backend/app/features/auth/service.py
      provides: Invite create/list/resend/revoke/accept orchestration and role assignment rules.
    - path: backend/tests/integration/auth/test_invitation_flow.py
      provides: End-to-end verification for invite lifecycle and transfer behavior.
  key_links:
    - from: backend/app/features/auth/routes.py
      to: backend/app/features/auth/service.py
      via: invitation endpoints delegating to service
      pattern: invitation
    - from: backend/app/features/auth/service.py
      to: backend/app/features/auth/models.py
      via: Invitation state transitions pending accepted expired revoked
      pattern: Invitation
    - from: backend/app/features/auth/service.py
      to: backend/app/features/auth/security.py
      via: signed invite token create/verify
      pattern: invite
---

<objective>
Deliver the full workspace invitation lifecycle with canonical role assignment and explicit transfer semantics.

Purpose: Satisfy AUTH-03 while enforcing locked role/authority decisions in AUTH-04.
Output: Invitation endpoints and service/state model with robust integration coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-RESEARCH.md
@.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-01-PLAN.md
@backend/app/features/auth/models.py
@backend/app/features/auth/schemas.py
@backend/app/features/auth/routes.py
@backend/app/features/auth/service.py

<interfaces>
From backend/app/features/auth/models.py:
```python
class Invitation(Base):
    id: Mapped[str]
    workspace_id: Mapped[str]
    email: Mapped[str]
    invited_by_user_id: Mapped[str]
    status: Mapped[str]
```

From backend/app/features/auth/schemas.py:
```python
class RegisterWorkspaceRequest(BaseModel):
    invited_members: list[EmailStr] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define invitation lifecycle contracts and failing tests</name>
  <files>backend/app/features/auth/schemas.py, backend/tests/integration/auth/test_invitation_flow.py</files>
  <behavior>
    - Test 1: create invite allowed only for Admin/Owner and PM/Team Lead and requires canonical role selection.
    - Test 2: duplicate invite in same workspace is rejected.
    - Test 3: pending invite list and resend/revoke/edit-role endpoints enforce role authority rules.
    - Test 4: invite expires after 7 days and cannot be accepted.
  </behavior>
  <action>Add invitation request/response schemas and write failing integration tests for create/list/resend/revoke/role-edit/accept flows before implementing service logic.</action>
  <verify>
    <automated>cd backend && pytest tests/integration/auth/test_invitation_flow.py -q</automated>
  </verify>
  <done>Invitation lifecycle expectations are fully encoded in failing tests and typed API schemas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement invitation state machine and acceptance logic</name>
  <files>backend/app/features/auth/models.py, backend/app/features/auth/service.py, backend/app/features/auth/security.py, backend/app/features/auth/routes.py</files>
  <behavior>
    - Test 1: pending to accepted expired revoked transitions are persisted with timestamps.
    - Test 2: existing-user acceptance from another workspace requires explicit transfer confirmation.
    - Test 3: same-workspace existing email cannot be invited/accepted as duplicate.
  </behavior>
  <action>Extend Invitation model with role, expiry, and lifecycle timestamp fields. Implement signed invite token generation/verification and lifecycle endpoints: create, list pending, resend, revoke, edit pending role, and accept. Enforce locked rules exactly: 7-day expiry, duplicate rejection for same workspace, allowed cross-workspace invite with explicit transfer confirmation, and redirect-target readiness for invited workspace dashboard.</action>
  <verify>
    <automated>cd backend && pytest tests/integration/auth/test_invitation_flow.py -q</automated>
  </verify>
  <done>Invitation API/service fully supports v1 lifecycle, transfer confirmation rules, and automated tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce role-change authority boundaries for invitations and activated users</name>
  <files>backend/app/features/auth/service.py, backend/app/features/auth/routes.py</files>
  <action>Ensure only Admin/Owner can change active-user roles post-activation, PM/Team Lead cannot change roles, and no self-role changes are allowed. Keep pending-invite role edits allowed only to authorized inviter roles, with Admin/Owner authority for final changes.</action>
  <verify>
    <automated>cd backend && pytest tests/integration/auth/test_invitation_flow.py -q</automated>
  </verify>
  <done>Authority matrix for role modifications matches locked decisions and is covered by integration tests.</done>
</task>

</tasks>

<verification>
- Run invitation integration suite and validate all state transitions and authority boundaries.
- Ensure 7-day expiry and explicit transfer-confirm requirement are asserted in tests.
</verification>

<success_criteria>
- AUTH-03 invitation lifecycle is fully implemented and test-covered.
- Invitation role assignment and active-role change authority constraints match locked decisions.
- Invite acceptance behavior cleanly separates new-user onboarding from existing-user transfer.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-roles-and-workspace-trust-boundary-week-1-2/01-02-SUMMARY.md`
</output>
